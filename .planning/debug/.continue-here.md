---
phase: ad-hoc-debug-ui-bugs
task: completed
status: human-verify-pending
last_updated: 2026-03-09T20:11:39.559Z
---

<current_state>
All 7 UI bugs were found, fixed, built, and installed to local VST3 path.
Awaiting user verification in DAW. The built VST3 is at:
`%LOCALAPPDATA%\Programs\Common\VST3\DIMEA ChordJoystick MK2.vst3`
Run `do-reinstall.ps1` in admin PowerShell if the system VST3 path also needs updating.

Debug session file: `.planning/debug/ui-bugs-gate-slider-crosshair-tempo-stars.md`
</current_state>

<completed_work>

- Bug 1 (Gate Length slider): `arpGateTimeKnob_` changed from `VelocityKnob` to `VelocitySlider` in `PluginEditor.h` — now responds to horizontal (left/right) drag
- Bug 2 (Crosshair in INV mode): Added voiceH0/H1/V0/V1 index variables that swap voice-to-segment assignment when invOn=true — lines now go from cursor to correct axis
- Bug 3 (Tempo knob size): Restructured layout to give Tempo knob same height as Probability/Density knobs (~56-58px)
- Bug 4 (BPM label): Kept BELOW the knob (user confirmed — do NOT move above). Size/spacing adjusted to match Density label style.
- Bug 5 (Knob smoothing): Confirmed NOT a bug — `randomFreeTempoKnob_` already uses `VelocityKnob` with EMA velocity-sensitive drag, same as LFO sliders. No change needed.
- Bug 6 (Stars condensed at corner): Fixed `bgRotPrev_` init — in `bgRotInitialized_` snap block, `bgRotPrev_` now also snaps to `newTarget * pi/180`, preventing a pi/2 drift heading spike on first tick
- Bug 7 (INV mode Mod Fix knobs): Added `filterXOffsetAtt_` / `filterYOffsetAtt_` to the INV attachment-swap block (was missing — only Mode combos and Atten knobs were swapped before). Now cross-wires correctly in INV mode.
</completed_work>

<remaining_work>

- User has NOT yet verified in DAW — all 7 fixes need live testing
- Possible follow-up bugs from any of the fixes (especially Bug 2 crosshair and Bug 7 Mod Fix)
</remaining_work>

<decisions_made>

- BPM label stays BELOW the knob (user corrected agent — agent had moved it above to match Density, user said keep it below)
- Knob smoothing: VelocityKnob already handles this; no new code needed
- Mod Fix INV fix: mirrored exact same APVTS swap pattern used for Atten knobs
</decisions_made>

<blockers>
- None blocking. Build succeeded. User needs to test in DAW.
</blockers>

<context>
This session was pure ad-hoc debugging via gsd-debugger agents. No active GSD phase.
The debug file `.planning/debug/ui-bugs-gate-slider-crosshair-tempo-stars.md` has full evidence log.
Previous active phase before this session was 43.2 (living-space starfield animations).
Phase 45 (arpeggiator step patterns UI redesign) appears to be next planned phase.
</context>

<next_action>
Load DAW, verify all 7 bugs are fixed:
1. ARP section: Gate Length slider responds to left/right drag
2. INV mode: crosshair lines go from cursor to correct axis
3. Random box: Tempo knob same size as Density/Probability
4. Random box: BPM label below the Tempo knob, matching Density label style
5. Random box with INV on: Mod Fix knobs can be dragged and hold their value (not snap back)
6. Random box with INV on: -/+ Mod knob causes Mod Fix knob to visually update
7. Joystick pad: stars spread across pad evenly, move with background (no corner bunching)

If any fail, resume with: /gsd:debug [describe what's wrong]
</next_action>
