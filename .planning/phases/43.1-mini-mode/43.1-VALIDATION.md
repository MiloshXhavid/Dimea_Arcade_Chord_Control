---
phase: 43.1
slug: mini-mode
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-10
---

# Phase 43.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual (JUCE VST3 UI — no automated window resize/visibility test harness) |
| **Config file** | none |
| **Quick run command** | Build: `cmake --build build --config Debug --target ChordJoystick_VST3` |
| **Full suite command** | Build + load in Reaper, exercise all 3 window modes |
| **Estimated runtime** | ~60 seconds (build) + ~2 min manual DAW verification |

---

## Sampling Rate

- **After every task commit:** Run quick build command — zero compile errors
- **After every plan wave:** Load plugin in Reaper, verify window transitions manually
- **Before `/gsd:verify-work`:** Full manual suite green (all 5 success criteria)
- **Max feedback latency:** ~120 seconds (build + reload)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 43.1-01-01 | 01 | 1 | WindowMode enum + member | build | `cmake --build build --config Debug --target ChordJoystick_VST3` | ✅ | ⬜ pending |
| 43.1-01-02 | 01 | 1 | applyWindowMode() skeleton | build | `cmake --build build --config Debug --target ChordJoystick_VST3` | ✅ | ⬜ pending |
| 43.1-01-03 | 01 | 1 | resized() gating | build | `cmake --build build --config Debug --target ChordJoystick_VST3` | ✅ | ⬜ pending |
| 43.1-01-04 | 01 | 1 | Header ◱ + ⛶ buttons | manual | Load in Reaper → click ◱ → window shrinks | ✅ | ⬜ pending |
| 43.1-01-05 | 01 | 1 | Overlay button (paintOverChildren + mouseDown) | manual | Mini mode → top-right overlay → returns to Full | ✅ | ⬜ pending |
| 43.1-01-06 | 01 | 1 | Maxi display detection | manual | Click ⛶ → window fills display as square | ✅ | ⬜ pending |
| 43.1-01-07 | 01 | 1 | Gamepad optMode 3/4 extension | build | `cmake --build build --config Debug --target ChordJoystick_VST3` | ✅ | ⬜ pending |
| 43.1-01-08 | 01 | 1 | savedUiScale_ guard | build + manual | Reload plugin → Full scale unchanged after Mini visit | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test files needed — all verification is build-pass + manual DAW exercise.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Mini: window shrinks to pad square at current scale | SC-1 | Window geometry only visible in live DAW host | Load in Reaper → click ◱ → measure window = padSize² |
| Maxi: window fills display as square | SC-2 | Display detection requires real monitor | Click ⛶ → verify window = min(display W, H) |
| Overlay button returns to Full in Mini | SC-3 | Requires live window state | Enter Mini → click top-right overlay → Full view restored |
| Overlay button returns to Full in Maxi | SC-3 | Requires live window state | Enter Maxi → click top-right overlay → Full view restored |
| Starfield/warp/cursor render in Mini + Maxi | SC-4 | Visual rendering only verifiable by eye | Move joystick → particles + cursor animate in both compact modes |
| Plugin opens in Full view on reload | SC-5 | Requires host reload | Close plugin in Mini → reopen → must open Full |
| Phase 43 scale factor preserved after Mini visit | SC-8 | Requires reload + state read | Visit Mini → return Full → reload → scale unchanged |
| Gamepad optMode 3 → Mini, 4 → Maxi | SC-6 | Requires physical/virtual gamepad | Cycle Option button 4x → mode 3 = MINI view; 5x = MAXI view |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
