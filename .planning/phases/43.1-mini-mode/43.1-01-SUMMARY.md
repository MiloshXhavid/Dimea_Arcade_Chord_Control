---
phase: 43.1-mini-mode
plan: 01
subsystem: ui
tags: [juce, cpp, window-mode, gamepad, vst3, plugin-editor]

# Dependency graph
requires:
  - phase: 43-resizable-ui
    provides: scaleFactor_ persistence, setResizeLimits/setFixedAspectRatio constrainer setup, savedUiScale_ atomic

provides:
  - WindowMode enum (Full/Mini/Maxi) state machine in PluginEditor
  - applyWindowMode() — constrainer adjustment + child visibility + setSize transition
  - getOverlayButtonBounds() — 24px return button in compact top-right corner
  - Compact-mode resized() branch (pad fills entire window, savedUiScale_ guarded)
  - Header Mini/Maxi buttons (◱/⛶) with hover state, drawn only in Full mode
  - paintOverChildren() overlay ↩ return button for Mini/Maxi
  - mouseMove()/mouseExit() hover detection for header buttons
  - GamepadInput 5-state cycle (0=off, 1=ARP, 2=INTRVL, 3=MINI, 4=MAXI)
  - resetOptionMode() public method on GamepadInput
  - D-pad dispatch restricted to modes 1 & 2 only (modes 3/4 use BPM/looper path)
  - timerCallback OPTION indicator extended to 5 modes with teal/amber colours
  - timerCallback applyWindowMode dispatch on mode 3/4 change

affects:
  - Phase 43.1 future plans (additional mini-mode features)
  - Any phase reading optionMode_ or isPresetScrollActive()

# Tech tracking
tech-stack:
  added: []
  patterns:
    - WindowMode enum dispatch in resized()/paint() for multi-layout plugin window
    - Constrainer adjustment before setSize to prevent aspect-ratio rejection
    - Child visibility bulk toggle (all except joystickPad_) for compact modes
    - isHoverMini_/isHoverMaxi_ repaint-on-change pattern for header hover buttons
    - lastHighlightMode_ change-detection driving applyWindowMode() from timerCallback

key-files:
  created: []
  modified:
    - Source/PluginEditor.h
    - Source/PluginEditor.cpp
    - Source/GamepadInput.h
    - Source/GamepadInput.cpp

key-decisions:
  - "applyWindowMode restores ALL children visible on return to Full (resized() then re-layouts them)"
  - "savedUiScale_.store() guarded inside Full block only — compact setSize never corrupts persisted scale"
  - "paint() early-returns in compact mode — JoystickPad child handles its own drawing"
  - "D-pad modes 3/4 fall through to BPM/looper else-branch — no new D-pad behavior needed for window modes"
  - "resetOptionMode() also resets lastHighlightMode_ to -1 so timerCallback forces Full transition on next tick"
  - "isPresetScrollActive() updated from != 0 to == 1 || == 2 — modes 3/4 must not trigger interval/octave highlight"

patterns-established:
  - "WindowMode dispatch: if (windowMode_ == Full) { ... } else { compact layout } — use in resized() and paint()"
  - "applyWindowMode: setResizeLimits + setFixedAspectRatio BEFORE setSize — order mandatory to avoid rejection"
  - "Overlay button: paintOverChildren for on-top visibility, mouseDown prepend for priority hit-test"

requirements-completed: []

# Metrics
duration: 35min
completed: 2026-03-10
---

# Phase 43.1 Plan 01: Mini Mode Summary

**3-state window mode system (Full/Mini/Maxi) with gamepad 5-state cycle — pad-only compact views via header buttons or Option modes 3/4**

## Performance

- **Duration:** ~90 min (including UAT cycle)
- **Started:** 2026-03-10T00:06:12Z
- **Completed:** 2026-03-10 (UAT approved)
- **Tasks:** 3 (2 auto + 1 UAT checkpoint, 2 deviation fixes)
- **Files modified:** 4

## Accomplishments
- WindowMode enum + applyWindowMode() state machine: constrainer adjustment, child visibility bulk toggle, setSize
- Compact mode resized() branch (pad fills window); savedUiScale_ guarded so Mini/Maxi visits don't corrupt scale
- Header ◱/⛶ buttons with hover feedback; overlay ↩ return button drawn in paintOverChildren()
- GamepadInput 5-state cycle (% 3 → % 5), D-pad dispatch restricted to modes 1 & 2, resetOptionMode() public method
- timerCallback: OPTION label extended to MINI (teal) / MAXI (amber); applyWindowMode dispatched on mode 3/4 change
- All 8 UAT checks approved: Mini shrink, Maxi expand, overlay return, joystick animation in compact, scale preservation, opens Full after session, gamepad modes 3/4, D-pad BPM in modes 3/4

## Task Commits

1. **Task 1: WindowMode state machine + applyWindowMode() + resized/paint/mouse integration** - `4769ac7` (feat)
2. **Task 2: Gamepad 5-state cycle + timerCallback window transitions + overlay optionMode_ reset** - `1cdcc56` (feat)
3. **Fix: Overlay button click-through via JoystickPad hitTest passthrough** - `b77da1a` (fix)
4. **Fix: Scale star count+size with window width in Maxi; high-quality bg upscale** - `974571e` (fix)
5. **Task 3: UAT verification** - human-approved

**Plan metadata:** (added with docs commit)

## Files Created/Modified
- `Source/PluginEditor.h` — WindowMode enum, windowMode_ member, isHoverMini_/isHoverMaxi_, applyWindowMode() + getOverlayButtonBounds() declarations, mouseMove/mouseExit overrides
- `Source/PluginEditor.cpp` — applyWindowMode() impl, resized() Full/compact dispatch, paint() early-return guard + header buttons, paintOverChildren() overlay button, mouseDown() prepend, mouseMove()/mouseExit() hover, timerCallback 5-mode extension
- `Source/GamepadInput.h` — resetOptionMode() public method, 5-mode comment, isPresetScrollActive() updated to == 1 || == 2
- `Source/GamepadInput.cpp` — % 3 → % 5 cycle, D-pad dispatch != 0 → == 1 || == 2

## Decisions Made
- Child visibility is bulk-toggled (all except joystickPad_) on compact entry; restored on Full return — resized() re-layouts via setBounds
- applyWindowMode sets constrainer BEFORE setSize to avoid aspect-ratio rejection of the new size
- savedUiScale_.store() moved inside the `if (windowMode_ == Full)` guard — compact mode setSize never overwrites the saved scale
- D-pad modes 3/4 fall through to the BPM/looper else-branch — no separate behavior needed for window-mode D-pad
- isPresetScrollActive() changed from `!= 0` to `== 1 || == 2` so modes 3/4 don't activate interval/octave highlight

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] resetOptionMode() added to GamepadInput.h before Task 1 commit**
- **Found during:** Task 1 build (mouseDown overlay handler referenced resetOptionMode())
- **Issue:** Plan specified resetOptionMode() as Task 2 work, but mouseDown in Task 1 calls it
- **Fix:** Added resetOptionMode() inline to GamepadInput.h during Task 1 (trivial 1-line method)
- **Files modified:** Source/GamepadInput.h
- **Verification:** Build passed cleanly after addition
- **Committed in:** 4769ac7 (Task 1 commit)

---

**2. [Rule 1 - Bug] JoystickPad overlay button click-through**
- **Found during:** Task 2 verification (UAT check 3 — overlay ↩ button)
- **Issue:** JoystickPad component sits on top of the overlay button hit area; mouseDown on editor never fired because JoystickPad consumed the event
- **Fix:** Added hitTest() override to JoystickPad returning false in compact mode, allowing parent editor to receive mouse events
- **Files modified:** Source/PluginEditor.cpp
- **Verification:** Overlay button click successfully calls applyWindowMode(Full) in Reaper UAT
- **Committed in:** b77da1a

**3. [Rule 1 - Bug] Stars too small in Maxi mode; background pixelated**
- **Found during:** UAT check 4 (starfield animation in compact modes)
- **Issue:** Star count/radius computed from Full-mode scaleFactor_ cap — at Maxi sizes stars appeared tiny; SpaceBackground upscaled with nearest-neighbour producing visible pixelation
- **Fix:** Star count/radius derived from getWidth() ratio in paint(); SpaceBackground drawn with highResamplingQuality
- **Files modified:** Source/PluginEditor.cpp
- **Verification:** Maxi mode shows proportionally sized starfield and sharp background in Reaper UAT
- **Committed in:** 974571e

---

**Total deviations:** 3 auto-fixed (1 Rule 3 ordering, 2 Rule 1 bugs)
**Impact on plan:** All fixes necessary for correct visual and interaction behavior. No scope creep.

## Issues Encountered
- Post-build MSB3073 errors from Reaper cache deletion script (Reaper was likely open) — not a compile error; VST3 built and installed successfully to both VST3 paths

## User Setup Required
None - UAT complete. Plugin installed and approved.

## Next Phase Readiness
- Mini/Maxi mode fully functional and UAT-approved; Phase 43.1 plan 01 complete
- Phase 43.2 (living space) can proceed; windowMode_ available as guard if needed
- No blockers

## Self-Check: PASSED
- SUMMARY.md: FOUND
- PluginEditor.h: FOUND
- PluginEditor.cpp: FOUND
- GamepadInput.h: FOUND
- GamepadInput.cpp: FOUND
- Task 1 commit 4769ac7: FOUND
- Task 2 commit 1cdcc56: FOUND
- Fix commit b77da1a: FOUND
- Fix commit 974571e: FOUND
- UAT: All 8 checks approved by human tester

---
*Phase: 43.1-mini-mode*
*Completed: 2026-03-10*
