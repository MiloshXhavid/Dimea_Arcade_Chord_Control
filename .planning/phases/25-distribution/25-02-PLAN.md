---
phase: 25-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe
autonomous: true

requirements:
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "GitHub pre-release tagged v1.5 exists at MiloshXhavid/Dima_Plugin_Chrdmachine with DIMEA-ChordJoystickMK2-v1.5-Setup.exe attached"
    - "Release title is exactly 'DIMEA PLUGIN - CHORD JOYSTICK MK2 v1.5' and is marked pre-release (v1.4 retains Latest status)"
    - "Release notes body contains sections: Routing, Arpeggiator, LFO Recording, Expression, Gamepad Option Mode 1, Bug Fix"
    - "Desktop folder C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/ contains three artifacts: installer EXE, VST3 bundle directory, source-v1.5.zip"
    - "The v1.5 git tag on the plugin remote points to HEAD (all Phase 24.1 docs included)"
  artifacts:
    - path: "installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe"
      provides: "Installer binary attached to GitHub pre-release"
    - path: "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/DIMEA-ChordJoystickMK2-v1.5-Setup.exe"
      provides: "Desktop backup copy of installer"
    - path: "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/source-v1.5.zip"
      provides: "Source archive of repo at v1.5 tag"
  key_links:
    - from: "installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe"
      to: "github.com/MiloshXhavid/Dima_Plugin_Chrdmachine/releases/tag/v1.5"
      via: "gh release create --repo MiloshXhavid/Dima_Plugin_Chrdmachine --prerelease"
      pattern: "gh release create v1.5"
    - from: "HEAD"
      to: "v1.5 tag on plugin remote"
      via: "git tag -a v1.5 + git push plugin v1.5"
      pattern: "git tag -a v1.5"
---

<objective>
Create the v1.5 git tag, push it to the plugin remote, publish the GitHub pre-release with the installer binary and full release notes, then copy all three artifacts to the versioned desktop backup folder.

Purpose: Phase 25 ships v1.5 publicly. The GitHub pre-release is the deliverable; the desktop backup is the local archive. This plan runs after smoke test approval from plan 01.
Output: GitHub pre-release at MiloshXhavid/Dima_Plugin_Chrdmachine/releases/tag/v1.5 with binary asset; Desktop/Dima_plug-in/v1.5/ with installer + VST3 bundle + source zip.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-distribution/25-CONTEXT.md
@.planning/phases/25-distribution/25-RESEARCH.md
@.planning/phases/25-distribution/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1.5 git tag and push to plugin remote</name>
  <files>installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe</files>
  <action>
    **Step 1 — Verify git remote name for the plugin repository.**

    The plugin remote was named `plugin` in Phase 16. Confirm it still exists:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" remote -v
    ```
    Look for `MiloshXhavid/Dima_Plugin_Chrdmachine` in the output. Note the remote name (expected: `plugin`). If not found, use the full URL directly in the push command below.

    **Step 2 — Check how far HEAD is ahead of the v1.4 tag.**

    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.4..HEAD --oneline
    ```
    Expect multiple commits (all Phase 17–24.1 work). This confirms the tag needs to be created at HEAD, not at v1.4.

    **Step 3 — Create an annotated v1.5 tag at HEAD.**

    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" tag -a v1.5 -m "v1.5 — Routing + Expression"
    ```

    **Step 4 — Push the tag to the plugin remote.**

    If the remote is named `plugin`:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" push plugin v1.5
    ```

    If the `plugin` remote was not found in Step 1, push by URL:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" push https://github.com/MiloshXhavid/Dima_Plugin_Chrdmachine.git v1.5
    ```

    **Step 5 — Verify the tag is now on the remote.**

    ```bash
    gh api repos/MiloshXhavid/Dima_Plugin_Chrdmachine/git/refs/tags/v1.5
    ```
    Must return a JSON object (not a 404) confirming the tag exists on the remote. If it returns 404, the push failed — check authentication and retry.
  </action>
  <verify>
    <automated>
    1. git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.5..HEAD --oneline (must return 0 lines — tag is at HEAD)
    2. gh api repos/MiloshXhavid/Dima_Plugin_Chrdmachine/git/refs/tags/v1.5 (must return JSON, not 404)
    </automated>
  </verify>
  <done>v1.5 annotated tag exists at current HEAD and is confirmed present on the MiloshXhavid/Dima_Plugin_Chrdmachine remote.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub pre-release and desktop backup</name>
  <files>
    C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/DIMEA-ChordJoystickMK2-v1.5-Setup.exe
    C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/source-v1.5.zip
  </files>
  <action>
    **Step 1 — Write the release notes to a temp file.**

    Use the Write tool to create `C:/Users/Milosh Xhavid/get-shit-done/installer/Output/release-notes-v1.5.md` with this content:

    ```markdown
    ## DIMEA CHORD JOYSTICK MK2 v1.5

    DIMEA CHORD JOYSTICK MK2 v1.5 is the most comprehensive update yet — adding single-channel MIDI routing, per-voice sub-octave doubling, a 6-mode arpeggiator, LFO recording, expanded random trigger modes, left joystick modulation targets, and Gamepad Option Mode 1 face-button dispatch. All features are backward-compatible with existing presets.

    ### New in v1.5

    **Routing**
    - Single-Channel mode: all voices → one user-selected MIDI channel (default is Multi-Channel, voices on ch 1–4)
    - Reference-counted note collisions in Single-Channel mode: note-off only fires when the last voice on a pitch releases
    - Sub-Octave per voice: toggle on any pad to fire a second note-on exactly 12 semitones below on every trigger

    **Arpeggiator**
    - Steps through the 4-voice chord at configurable Rate (1/4, 1/8, 1/16, 1/32) and Order (Up, Down, Up-Down, Random)
    - Unified Gate Length parameter shared with Random trigger source
    - Bar-boundary launch: when DAW sync is active, Arp waits for the next bar before stepping begins

    **LFO Recording**
    - Arm button lets you record one loop cycle of LFO output into a ring buffer
    - After one cycle, LFO switches to playback — replaying stored values in sync with the looper
    - Distortion stays adjustable during playback; Shape/Freq/Phase/Level/Sync are locked
    - Clear button returns to normal LFO mode

    **Expression**
    - Random trigger sources split into Random Free (continuous) and Random Hold (fires only while pad is held)
    - Population knob (1–64) and Probability knob (0–100%) replace the old RND Density/Gate controls
    - Subdivision options extended to include 1/64
    - Left Joystick X and Y can target: Filter Cutoff, Filter Resonance, LFO Freq, LFO Phase, LFO Level, or Gate Length
    - LFO sliders visually track the joystick in real time; releasing the stick returns to the user-set base value

    **Gamepad — Option Mode 1**
    - Circle: toggle Arp on/off (resets looper to beat 0 on turn-on; arms if DAW sync active)
    - Triangle: step Arp Rate up (1 press = step up; 2 quick = step down)
    - Square: step Arp Order (same two-press architecture)
    - X: toggle RND Sync
    - Holding pad button (R1/R2/L1/L2) + R3: toggle Sub-Octave for that voice (any mode)

    **Bug Fix**
    - Fixed: Ableton MIDI port startup crash when plugin is present in default template

    ### Install

    1. Download `DIMEA-ChordJoystickMK2-v1.5-Setup.exe`
    2. Run the installer — read the welcome screen note about Windows SmartScreen
    3. Admin privileges required (installs to the system VST3 folder)
    4. Rescan plugins in your DAW

    ### Requirements

    - Windows 10/11 64-bit
    - VST3-compatible DAW (Ableton Live, REAPER, etc.)
    - Xbox / PS4 / PS5 controller optional (for gamepad input)
    ```

    **Step 2 — Create the GitHub pre-release.**

    CRITICAL: Use `--repo MiloshXhavid/Dima_Plugin_Chrdmachine` — the plugin remote, NOT the GSD framework origin. CRITICAL: Use `--prerelease` — v1.5 is a pre-release; omitting this flag makes it a full release and bumps v1.4 from "Latest" incorrectly.

    ```bash
    gh release create v1.5 \
      --repo MiloshXhavid/Dima_Plugin_Chrdmachine \
      --title "DIMEA PLUGIN - CHORD JOYSTICK MK2 v1.5" \
      --prerelease \
      --notes-file "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/release-notes-v1.5.md" \
      "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe#DIMEA-ChordJoystickMK2-v1.5-Setup.exe"
    ```

    The `#DIMEA-ChordJoystickMK2-v1.5-Setup.exe` suffix after the asset path sets the display name on the GitHub release page.

    Verify the release was created correctly:
    ```bash
    gh release view v1.5 --repo MiloshXhavid/Dima_Plugin_Chrdmachine
    ```
    Confirm: title matches, isPrerelease is true, asset listed.

    Also confirm v1.4 still shows as Latest (v1.5 should NOT be Latest):
    ```bash
    gh release list --repo MiloshXhavid/Dima_Plugin_Chrdmachine
    ```

    **Step 3 — Create the desktop backup folder.**

    Check if v1.5 folder already exists (it should not based on research, but follow the CONTEXT.md rule):
    ```bash
    ls "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5" 2>/dev/null && echo EXISTS || echo NOT_EXISTS
    ```
    - If NOT_EXISTS: create `v1.5\`
    - If EXISTS: create `v1.5.1\` instead (do not overwrite)

    ```bash
    mkdir -p "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5"
    ```
    (Adjust to `v1.5.1` if EXISTS case applies.)

    **Step 4 — Copy installer to desktop backup.**

    ```bash
    cp "C:/Users/Milosh Xhavid/get-shit-done/installer/Output/DIMEA-ChordJoystickMK2-v1.5-Setup.exe" \
       "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/"
    ```

    **Step 5 — Copy VST3 bundle to desktop backup.**

    Copy from the build output (not from the system VST3 path — the build output is guaranteed to match what was compiled into the installer):
    ```bash
    cp -r "C:/Users/Milosh Xhavid/get-shit-done/build/ChordJoystick_artefacts/Release/VST3/DIMEA CHORD JOYSTICK MK2.vst3" \
          "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/"
    ```

    **Step 6 — Create the source archive.**

    Tag must exist before archiving (Task 1 created it). Archive from the v1.5 tag:
    ```bash
    git -C "C:/Users/Milosh Xhavid/get-shit-done" archive \
      --format=zip \
      --prefix=ChordJoystick-v1.5/ \
      v1.5 \
      -o "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/source-v1.5.zip"
    ```

    **Step 7 — Verify all three desktop backup artifacts exist.**

    ```bash
    ls -la "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/"
    ```
    Expected: DIMEA-ChordJoystickMK2-v1.5-Setup.exe, DIMEA CHORD JOYSTICK MK2.vst3/ (directory), source-v1.5.zip
  </action>
  <verify>
    <automated>
    1. gh release view v1.5 --repo MiloshXhavid/Dima_Plugin_Chrdmachine --json title,isPrerelease,assets (title must be "DIMEA PLUGIN - CHORD JOYSTICK MK2 v1.5", isPrerelease must be true, assets must include DIMEA-ChordJoystickMK2-v1.5-Setup.exe)
    2. ls "C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/" (must list all three: EXE, VST3 bundle dir, source zip)
    </automated>
  </verify>
  <done>GitHub pre-release v1.5 published at MiloshXhavid/Dima_Plugin_Chrdmachine with DIMEA-ChordJoystickMK2-v1.5-Setup.exe attached and full release notes. v1.4 retains Latest status. Desktop backup at Dima_plug-in/v1.5/ contains all three artifacts.</done>
</task>

</tasks>

<verification>
- `gh release view v1.5 --repo MiloshXhavid/Dima_Plugin_Chrdmachine` — pre-release exists with title "DIMEA PLUGIN - CHORD JOYSTICK MK2 v1.5" and binary asset attached
- `gh release list --repo MiloshXhavid/Dima_Plugin_Chrdmachine` — v1.5 shows "(Pre-release)", v1.4 retains "Latest"
- Release notes body contains: Routing, Arpeggiator, LFO Recording, Expression, Gamepad Option Mode 1, Bug Fix sections
- `ls C:/Users/Milosh Xhavid/Desktop/Dima_plug-in/v1.5/` — three artifacts present
- `git -C "C:/Users/Milosh Xhavid/get-shit-done" log v1.5..HEAD --oneline` — no output (tag at HEAD)
</verification>

<success_criteria>
- GitHub pre-release tagged v1.5 exists at MiloshXhavid/Dima_Plugin_Chrdmachine with DIMEA-ChordJoystickMK2-v1.5-Setup.exe attached
- Release is marked pre-release; v1.4 retains "Latest" tag on GitHub
- Release title is exactly "DIMEA PLUGIN - CHORD JOYSTICK MK2 v1.5"
- Release notes include all 6 feature groupings from CONTEXT.md (no SmartScreen note in notes body — it is in the installer)
- Desktop/Dima_plug-in/v1.5/ contains: DIMEA-ChordJoystickMK2-v1.5-Setup.exe, DIMEA CHORD JOYSTICK MK2.vst3/, source-v1.5.zip
- DIST-03 satisfied: GitHub pre-release with binary and structured notes
- DIST-04 satisfied: Desktop backup with all three artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/25-distribution/25-02-SUMMARY.md`
</output>
