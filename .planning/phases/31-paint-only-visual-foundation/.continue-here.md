---
phase: 31-paint-only-visual-foundation
plan: 02
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-04T15:29:55Z
---

<current_state>
Session completed two things: (1) fixed a crash-on-open bug, (2) overhauled the JoystickPad space visuals per user reference image.

The Release build compiled successfully but **could not be auto-installed** because the β character in the plugin name corrupts through bash. The user needs to run `do-reinstall.ps1` manually from PowerShell to install, then do visual verification.
</current_state>

<completed_work>

- Plan 31-01 fully done (all commits from previous session)
- Plan 31-02 Tasks 1+2 done (glowPhase_ + beat wire, from previous session)
- **CRASH FIX**: `JoystickPad::paint()` was calling `getRawParameterValue("randomDensity")` which returned nullptr (parameter was renamed to "randomPopulation"). Fixed to `"randomPopulation"` with correct 1–64 jmap range. Built and installed. DAWs now open without crashing.
- **Visual overhaul** (this session):
  - Milky way: added along-band sine harmonics (3 frequencies) for organic clumpy texture; reduced layer contributions (outer 0.09→0.05, mid 0.50→0.28, core 1.0→0.70); alpha multiplier 1.9→0.85 so band is a soft haze not a solid bar
  - Stars: 350 stars, size 0.20–1.1px (power 4.5 — almost all sub-pixel), alpha 10–130 (heavy power-law dim), 88% white / 12% faint blue-white, no yellow — matches reference
  - Heatmap removed: Layer 4 red→blue radial gradient deleted from both resized() bake and paint() draw; only faint white circle outline remains
  - Probability knob → galaxy band brightness (0.05–0.55 opacity range)
  - Population knob → star count only (was driving both count and brightness)
  - Cursor glow: color changed from pink-red (Clr::highlight) to cyan #00CFFF; alpha reduced to 0.04–0.15 (barely visible); glow advance now BPM-locked: `bpm / 3600.0f` per timer tick = 1 cycle per beat; dark halo alpha 0.65→0.45, radius 8→6
- Build compiled: 0 errors, 1 warning (LNK4232 β character in DLL name — cosmetic, harmless)
</completed_work>

<remaining_work>

- **INSTALL**: Run `.\do-reinstall.ps1` from PowerShell as admin (or just copy manually — see below)
- Plan 31-02 Task 3: Human visual verification — confirm space visuals look like reference
- Write `31-02-SUMMARY.md`
- Update `.planning/STATE.md` to mark phase 31 complete
- Commit summary + state
</remaining_work>

<decisions_made>

- **Probability knob drives galaxy brightness** — user explicitly requested this mapping
- **Population knob drives star count only** — decoupled from brightness
- **Heatmap fully removed** — user said "remove the colours inside the circle that go from red to blue"
- **Cyan glow #00CFFF** — user wants cursor glow to match logo aesthetic (cyan blue), very subtle
- **BPM-locked pulse** — glow cycle = 1 beat, uses `proc_.getEffectiveBpm()` (looper BPM or DAW BPM when synced)
- **do-reinstall.ps1 is the reliable install path** — β character corrupts through bash; the existing script handles this correctly
</decisions_made>

<blockers>

- **Install blocked by β character in bash**: The auto-install in the build step fails because bash corrupts the β in "Joystick Chord Control (β-test).vst3". The .vst3 binary IS built at `build\ChordJoystick_artefacts\Release\VST3\Joystick Chord Control (β-test).vst3`. Fix: run `.\do-reinstall.ps1` from PowerShell — it uses LiteralPath and handles the name correctly. Note: `do-reinstall.ps1` line 4 points to the old name "Chord Joystick Controller (BETA).vst3" — update that line to "Joystick Chord Control (β-test).vst3" first, or just use `.\install-beta.ps1` which was created this session (same issue with β in bash though — run from PowerShell directly).
</blockers>

<context>
Phase 31 is about making the JoystickPad look like deep space. The reference image (Desktop/Screenshot 2026-03-03 145547.png) shows: pitch-black background, faint diagonal milky way haze, tiny white star pinpricks, no colored overlays. The heatmap was a mistake from the original plan — user confirmed it should be removed.

The visual layer stack in paint() is now:
1. Near-black background (#05050f)
2. Milky way band (baked ARGB image, prob-driven opacity)
3. Stars (350 fillEllipse calls, pop-driven count)
4. Range circle outline only (no heatmap)
5. Semitone grid (dashed horiz + solid vert, scale-aware brightness)
6. Particles (gold movement dust + voice burst colors)
7a. Center reference dot
7b. Cyan glow ring (BPM-breathing, barely visible)
7c. Cursor dot (pink Clr::highlight) + ticks
8. Border

To install: open PowerShell as admin, navigate to project root, run:
```
.\do-reinstall.ps1
```
(Update line 4 buildSrc to "Joystick Chord Control (β-test).vst3" if not already done)
</context>

<next_action>
1. Run `.\do-reinstall.ps1` from PowerShell as admin to install the built binary
2. Load plugin in DAW, verify space visuals match the reference image
3. If approved: write 31-02-SUMMARY.md, update STATE.md phase 31 → complete, commit
</next_action>
