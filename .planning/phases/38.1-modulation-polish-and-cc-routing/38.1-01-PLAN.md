---
phase: 38.1-modulation-polish-and-cc-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/PluginProcessor.cpp
  - Source/PluginEditor.cpp
autonomous: true

requirements:
  - MOD-FIX-01
  - CC-ROUTE-01

must_haves:
  truths:
    - "MOD FIX knob offsets all LFO target sliders (Freq, Phase, Level, cross-axis variants) when gamepad is connected and stick is centered"
    - "filterXMode and filterYMode offer 18 named CC options (CC1–CC93 matching lfoXCcDest list) instead of 4 hardcoded CCs"
    - "LFO/Gate modes shift to correct indices after CC list expansion; no regression on existing LFO routing"
    - "kFilterCcNums[18] lookup replaces all hardcoded ternary chains in processBlock"

objective: >
  Fix the MOD FIX liveGamepad dispatch missing 8 of 10 LFO target cases,
  then expand filterXMode/filterYMode CC options from 4 to 18 named CCs.
---

## Tasks

1. **MOD FIX liveGamepad offset fix**
   - Add `(xOffset/50)*scale` / `(yOffset/50)*scale` to every missing case in the liveGamepad dispatch block in `PluginProcessor.cpp`
   - Cases: xMode 5, 6, 8, 9, 10 and yMode 5, 6, 8, 9, 10

2. **Expand CC routing to 18 options**
   - Replace hardcoded 4-CC ternary chains with `kFilterCcNums[mode - kNumLfoTargetModes]` lookup
   - Update `PluginEditor.cpp` combo population to list all 18 CCs by name
   - Shift LFO/Gate mode indices accordingly
