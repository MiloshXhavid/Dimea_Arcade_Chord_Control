---
phase: 17-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified: []
autonomous: false
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "pluginval level 5 completes with 0 new failures (same or fewer failures than v1.4 baseline)"
    - "Recording a 4-bar loop and letting it play 3+ cycles shows beat 1 landing on the DAW grid with no audible drift"
    - "Disconnecting the PS4 via Bluetooth and reconnecting does not crash the plugin — the controller resumes within one reconnect cycle"
  artifacts:
    - path: "pluginval_output.txt"
      provides: "pluginval level 5 run log confirming no new failures"
      contains: "ChordJoystick"
  key_links:
    - from: "pluginval run"
      to: "VST3 binary"
      via: "pluginval --strictness-level 5 --validate-in-process"
      pattern: "strictness-level 5"
---

<objective>
Verify that both BUG-01 (looper anchor drift) and BUG-02 (BT reconnect crash) are fully fixed by running pluginval level 5 and a manual smoke test covering both scenarios.

Purpose: Confirms the codebase is stable before Phase 18 (Single-Channel Routing) builds on top of it.
Output: pluginval passes, manual smoke test confirms both bugs are gone, phase is complete.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-bug-fixes/17-01-SUMMARY.md
@.planning/phases/17-bug-fixes/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build final VST3 and run pluginval level 5</name>
  <files></files>
  <action>
Build the VST3 in Release configuration and run pluginval at strictness level 5.

Step 1 — Build Release VST3:
```
cmake --build "C:/Users/Milosh Xhavid/get-shit-done/build" --config Release --target ChordJoystick_VST3 -j4
```

Step 2 — Locate the built VST3. It will be at one of:
- `C:/Users/Milosh Xhavid/get-shit-done/build/ChordJoystick_artefacts/Release/VST3/ChordJoystick.vst3`
- `C:/Users/Milosh Xhavid/AppData/Local/Programs/Common/VST3/ChordJoystick.vst3` (if COPY_PLUGIN_AFTER_BUILD succeeded)

Step 3 — Run pluginval (binary is at `C:/Users/Milosh Xhavid/get-shit-done/pluginval/pluginval.exe`):
```
"C:/Users/Milosh Xhavid/get-shit-done/pluginval/pluginval.exe" \
  --strictness-level 5 \
  --validate-in-process \
  --output-dir "C:/Users/Milosh Xhavid/get-shit-done" \
  "C:/Users/Milosh Xhavid/get-shit-done/build/ChordJoystick_artefacts/Release/VST3/ChordJoystick.vst3" \
  2>&1 | tee pluginval_output.txt
```

Accept result if: "Tests complete: N tests run, 0 failed" where N >= the v1.4 baseline count. If pluginval is not at the expected path, search in `C:/Users/Milosh Xhavid/get-shit-done/pluginval/`.

If the COPY target fails due to permissions (known issue per project memory), run:
```
powershell -ExecutionPolicy Bypass -File "C:/Users/Milosh Xhavid/get-shit-done/fix-install.ps1"
```
then re-run pluginval pointing at the installed path.

Save pluginval output to `pluginval_output.txt` in the project root.
  </action>
  <verify>
    <automated>"C:/Users/Milosh Xhavid/get-shit-done/pluginval/pluginval.exe" --version 2>&amp;1 | head -2 &amp;&amp; grep -i "0 failed\|Tests complete" "C:/Users/Milosh Xhavid/get-shit-done/pluginval_output.txt" 2>/dev/null || echo "Run pluginval first"</automated>
  </verify>
  <done>pluginval_output.txt exists, contains "Tests complete" with 0 failures. Build is Release.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Both bug fixes applied (17-01 looper anchor, 17-02 BT reconnect guard), pluginval passed automatically. Manual smoke test needed to confirm real-world behavior.
  </what-built>
  <how-to-verify>
Load the built VST3 in your DAW (Ableton). Run both smoke tests:

**Smoke Test 1 — BUG-01 looper anchor (estimated 3 min):**
1. Enable DAW sync in the plugin (Sync button on)
2. Set loop length to 4 bars, 4/4 time
3. Start DAW transport at bar 1 beat 1
4. Press Record on the looper — let it record a full 4 bars (play a chord pattern with the joystick)
5. Let the loop play back for at least 3 complete cycles
6. Watch the DAW grid vs the looper output: beat 1 of each cycle should land exactly on the DAW grid beat 1
7. PASS: No audible drift or late-start slip between cycles
8. FAIL: Beat 1 arrives slightly late on cycle 2+ (old behaviour was ~1 block = ~10ms late)

**Smoke Test 2 — BUG-02 BT reconnect (estimated 2 min):**
1. Connect PS4 controller via Bluetooth — confirm "DualShock 4" appears in the plugin's gamepad status label
2. Hold the PS button on the controller > select "Turn Off Controller" — controller disconnects
3. Wait 5 seconds
4. Press the PS button again to reconnect via BT
5. PASS: Plugin does not crash; controller reconnects and the gamepad status label updates; joystick and buttons work normally
6. FAIL: Plugin crashes or freezes during reconnect
  </how-to-verify>
  <resume-signal>Type "approved" if both smoke tests pass, or describe which test failed and what you observed</resume-signal>
</task>

</tasks>

<verification>
Phase 17 complete when:
- All 13 Catch2 looper TCs pass (TC 13 added as BUG-01 regression)
- Plugin VST3 builds in Release with 0 errors
- pluginval level 5 passes with 0 new failures
- Human smoke test confirms looper beat alignment and BT reconnect stability
</verification>

<success_criteria>
- pluginval_output.txt shows 0 failures at strictness level 5
- Looper anchor: beat 1 of playback stays grid-locked across 3+ cycles after recording
- BT reconnect: no crash, controller resumes within one reconnect cycle
- Codebase ready for Phase 18 (Single-Channel Routing)
</success_criteria>

<output>
After completion, create `.planning/phases/17-bug-fixes/17-03-SUMMARY.md` describing:
- pluginval result (N tests, 0 failures)
- Smoke test outcomes for both BUG-01 and BUG-02
- Phase 17 status: complete
</output>
