---
phase: 38.2-custom-cc-routing
plan: 01
subsystem: testing
tags: [catch2, tdd, cc-routing, custom-cc, tdd-red]

# Dependency graph
requires: []
provides:
  - "Failing Catch2 test stubs for ccDestToNumber (custom index 19) and resolveFilterCcMode (custom mode 25)"
  - "Tests/CustomCcRoutingTests.cpp registered in ChordJoystickTests CMake target"
affects:
  - 38.2-02  # Plan 02 GREEN: create CustomCcRoutingHelpers.h and make tests pass

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD Wave 0: test file includes a header that does not yet exist, producing intentional compile-error RED state"

key-files:
  created:
    - Tests/CustomCcRoutingTests.cpp
  modified:
    - CMakeLists.txt

key-decisions:
  - "Tests include ../Source/CustomCcRoutingHelpers.h (not yet created) — compile error is the RED gate, not a runtime failure"
  - "ccDestToNumber and resolveFilterCcMode extracted to a dedicated header in Plan 02 to keep production logic unit-testable"

patterns-established:
  - "Wave 0 RED pattern: create test file with #include of a not-yet-existing header so the build itself is the RED signal"

requirements-completed: []

# Metrics
duration: 1min
completed: 2026-03-08
---

# Phase 38.2 Plan 01: Custom CC Routing Summary

**Wave 0 TDD RED stubs: three Catch2 test cases for ccDestToNumber and resolveFilterCcMode written and registered in CMakeLists.txt, compile intentionally fails until Plan 02 creates CustomCcRoutingHelpers.h**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-03-08T18:11:19Z
- **Completed:** 2026-03-08T18:11:56Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Created `Tests/CustomCcRoutingTests.cpp` with three failing TEST_CASE blocks (CC-CUSTOM-01, CC-CUSTOM-02, CC-CUSTOM-03)
- Registered `CustomCcRoutingTests.cpp` in the `ChordJoystickTests` `add_executable` source list in `CMakeLists.txt`
- Established clear RED gate: tests will not compile until `Source/CustomCcRoutingHelpers.h` is created in Plan 02

## Task Commits

Each task was committed atomically:

1. **Task 1+2: Create test stubs and register in CMake** - `8b4fe83` (test)

**Plan metadata:** (pending final commit)

_Note: Tasks 1 and 2 are committed together because they form a single atomic unit — the file and its CMake registration are inseparable for RED verification._

## Files Created/Modified
- `Tests/CustomCcRoutingTests.cpp` - Three Catch2 TEST_CASEs that #include ../Source/CustomCcRoutingHelpers.h (RED until Plan 02)
- `CMakeLists.txt` - CustomCcRoutingTests.cpp added to ChordJoystickTests add_executable source list (line 69)

## Decisions Made
- Tests include a header that does not yet exist (`../Source/CustomCcRoutingHelpers.h`) — this produces a compile error that serves as the TDD RED gate. No runtime failure path needed.
- Both tasks committed in a single commit because the test file without its CMake registration is not useful in isolation.

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 02 (GREEN) can now proceed: create `Source/CustomCcRoutingHelpers.h` with inline `ccDestToNumber` and `resolveFilterCcMode` functions, add APVTS params (`lfoXCustomCc`, `lfoYCustomCc`, `filterXCustomCc`, `filterYCustomCc`), update production code, verify all three tests pass.
- CMakeLists.txt already wired — no further CMake changes needed for test infrastructure.

---
*Phase: 38.2-custom-cc-routing*
*Completed: 2026-03-08*
