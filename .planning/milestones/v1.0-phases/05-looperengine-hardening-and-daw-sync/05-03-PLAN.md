---
phase: 05-looperengine-hardening-and-daw-sync
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [LOOPER-03, LOOPER-04]

must_haves:
  truths:
    - "Plugin loads in Reaper without crash"
    - "User can record a 4-bar 4/4 gate loop and play it back correctly in Reaper"
    - "SYNC ON: looper start aligns to bar boundary when DAW is rolling"
    - "Punch-in: record a second pass, previously unrecorded beats preserved"
    - "No std::mutex remains in LooperEngine audio-thread call stack"
  artifacts:
    - path: "Source/LooperEngine.h"
      provides: "Lock-free class (verified by build)"
      contains: "AbstractFifo"
    - path: "Source/LooperEngine.cpp"
      provides: "Lock-free implementation (verified by test suite)"
      contains: "deleteRequest_.exchange"
  key_links:
    - from: "PluginEditor [REC GATES] button"
      to: "looper_.recGates_ atomic"
      via: "proc_.looperSetRecGates() onClick"
      pattern: "looperSetRecGates"
    - from: "PluginEditor [SYNC] button"
      to: "looper_.syncToDaw_ atomic"
      via: "proc_.looperSetSyncToDaw() onClick"
      pattern: "looperSetSyncToDaw"
---

<objective>
Build the final Release VST3, install it, and run structured DAW verification tests in Reaper. Optionally verify in Ableton (best-effort — known crash blocker from Phase 01 may still apply).

Purpose: The lock-free engine and new UI buttons must be validated in real DAW conditions. The 4-bar loop test, SYNC alignment, and punch-in confirm the LooperEngine works end-to-end. This is the phase completion gate.

Output: Human-verified looper working in Reaper. Phase 05 complete.
</objective>

<execution_context>
@C:/Users/Milosh Xhavid/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Milosh Xhavid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-looperengine-hardening-and-daw-sync/05-CONTEXT.md
@.planning/phases/05-looperengine-hardening-and-daw-sync/05-01-SUMMARY.md
@.planning/phases/05-looperengine-hardening-and-daw-sync/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Release VST3 and run full test suite</name>
  <files>N/A — build-only task, no source file edits</files>
  <action>
Step 1 — Reconfigure and build Release VST3:
  cmake -B "C:/Users/Milosh Xhavid/get-shit-done/build" -S "C:/Users/Milosh Xhavid/get-shit-done" -G "Visual Studio 18 2026"
  cmake --build "C:/Users/Milosh Xhavid/get-shit-done/build" --config Release --target ChordJoystick_VST3 2>&amp;1 | tail -20

Step 2 — Confirm VST3 installed. The build should copy it automatically via COPY_PLUGIN_AFTER_BUILD. If it fails (requires elevated process), read the build output and note the target path. The VST3 bundle typically lands at:
  C:\Program Files\Common Files\VST3\ChordJoystick.vst3
If the copy fails, manually note the path from the build output — the user will copy it manually in the checkpoint.

Step 3 — Run all Catch2 tests (Debug config has tests, Release may not — run Debug):
  cmake --build "C:/Users/Milosh Xhavid/get-shit-done/build" --config Debug --target ChordJoystickTests
  ctest --test-dir "C:/Users/Milosh Xhavid/get-shit-done/build" --build-config Debug --output-on-failure 2>&amp;1

Step 4 — Verify no mutex in audio-thread path:
  grep -rn "std::mutex\|lock_guard\|eventsMutex_" "C:/Users/Milosh Xhavid/get-shit-done/Source/LooperEngine.h" "C:/Users/Milosh Xhavid/get-shit-done/Source/LooperEngine.cpp"
  Expected: zero matches.

Capture all output and report pass/fail counts to the user before the checkpoint.
  </action>
  <verify>
Release build completes with 0 errors. ctest reports all tests passed (0 failures). grep for mutex/lock_guard in LooperEngine.h/.cpp returns nothing.
  </verify>
  <done>Release VST3 built. All Catch2 tests pass. No mutex in LooperEngine audio path. Build path and VST3 install status reported to user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Lock-free LooperEngine with:
- AbstractFifo + double-buffer design (no std::mutex anywhere in audio path)
- [REC JOY] button: arms joystick X/Y recording (sparse, threshold-filtered)
- [REC GATES] button: arms gate (note-on/off) recording
- [SYNC] button: toggle SYNC ON/OFF — SYNC ON snaps loop start to nearest bar boundary using DAW ppqPosition
- capReached indicator: [REC JOY]/[REC GATES] buttons flash "CAP!" when 2048-event buffer is full
- Punch-in merge: new events replace old only at touched beat windows; untouched positions preserved
- Live input passthrough: looper gate playback emits MIDI directly (bypasses TriggerSystem) so live pads always work alongside looper playback
- All 26 Catch2 tests passing (15 original + 11 new LooperEngine tests including punch-in correctness test)
  </what-built>
  <how-to-verify>
Open Reaper. If VST3 was not auto-installed, copy ChordJoystick.vst3 from the build output path to C:\Program Files\Common Files\VST3\ (may need administrator rights), then rescan plugins in Reaper (Options > Preferences > VST).

Run these 4 tests in order. Report pass/fail for each:

--- TEST 1: Plugin loads ---
1. Insert ChordJoystick on a MIDI track in Reaper
2. Open the plugin UI
3. Confirm new buttons visible: [REC JOY], [REC GATES], [SYNC] alongside existing looper buttons
PASS: Plugin opens, 3 new buttons visible, no crash

--- TEST 2: 4-bar gate loop record + playback (SYNC OFF) ---
1. Set loop length to 4 bars (4/4), ensure SYNC button is NOT toggled
2. Press [PLAY] on the looper (not DAW transport)
3. Press [REC GATES] to arm gate recording
4. Press [REC] to start recording
5. Trigger voice 0 (ROOT pad) at beat 1, 3 of each bar (4 times total over 4 bars)
6. Press [REC] again to stop recording — looper switches to playback
7. Observe: gate LED for voice 0 flashes at the recorded beat positions, repeating every 4 bars
PASS: Voice 0 gate fires at the correct beat positions on each loop cycle

--- TEST 3: SYNC ON — bar-boundary alignment ---
1. Start DAW transport (set Reaper BPM to 120)
2. With DAW rolling at bar 3, beat 2 (ppq ≈ 9.5), press [SYNC] to enable sync
3. Press [PLAY] on looper
4. Press [REC GATES], then [REC] to start recording
5. Record 2 bars of gate hits
6. Press [REC] to stop — playback begins
7. Observe: looper playhead display starts from the bar boundary (ppq 8.0 = bar 3 in 4/4), not from ppq 9.5
PASS: Loop aligns to bar start, not to the exact ppqPosition when play was pressed

--- TEST 4: Punch-in replaces events at re-recorded positions ---
1. Complete TEST 2 first (4-bar loop recorded on voice 0)
2. Press [PLAY] to stop looper, then press [REC GATES] and [REC] again
3. Trigger voice 1 (THIRD pad) at beats you did NOT use in TEST 2 (e.g. beats 2, 4)
4. Press [REC] to finalise
5. Press [PLAY] to start looper
6. Observe: voice 0 events from original pass are still present; voice 1 events from new pass also fire
PASS: Both sets of events play back — original voice 0 events preserved at positions not overwritten

--- BONUS TEST (best-effort, do not block phase on failure): Ableton ---
1. Open Ableton Live 11 or 12
2. Attempt to load ChordJoystick on a MIDI track
3. If it loads without crash: run TEST 2 (4-bar loop)
4. If Ableton crashes on load: note "Ableton crash blocker still active" — Phase 06 will address it
PASS (bonus): Ableton loads plugin and 4-bar loop works
ACCEPTABLE FAIL: Ableton crashes — document and defer to Phase 06
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 Reaper tests pass (Ableton optional). Or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 05 complete when:
1. All Catch2 tests pass (0 failures, 26 total)
2. No std::mutex in LooperEngine.h or LooperEngine.cpp
3. Reaper tests 1-4 all pass (human-verified)
4. Ableton result documented (pass or "crash blocker still active")
</verification>

<success_criteria>
- Lock-free LooperEngine verified in real DAW via Reaper
- 4-bar gate loop records and plays back correctly
- SYNC ON aligns to bar boundary
- Punch-in preserves unmodified events
- Phase 05 status updated in ROADMAP.md and STATE.md as COMPLETE
</success_criteria>

<output>
After checkpoint approval:
1. Create `.planning/phases/05-looperengine-hardening-and-daw-sync/05-03-SUMMARY.md` with:
   - Reaper test results (pass/fail for each test)
   - Ableton result (pass or deferred)
   - Any issues found and how they were resolved
   - Final test count

2. Update `.planning/STATE.md`:
   - Current Phase: 05 COMPLETE
   - Progress bar for Phase 05 to [██████████]
   - Add to "What's Been Built": lock-free LooperEngine summary, [REC JOY]/[REC GATES]/[SYNC] buttons, DAW verification results
   - Remove "std::mutex in LooperEngine processBlock" from Known Issues

3. Update `.planning/ROADMAP.md`:
   - Phase 05 status: COMPLETE (3/3 plans done)
   - Mark all 3 plan checkboxes as [x]
</output>
